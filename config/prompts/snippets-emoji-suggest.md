Suggest Unicode emoji that match a name or a short description.

Return **5 to 8** distinct emoji variants, one emoji per line.
No words, no quotes, no markdown, no numbering, no explanation.

Rules:

- Each line is a single emoji (ZWJ sequences and flags are allowed).
- Prefer simple, widely supported emoji that clearly match the query.
- Offer useful alternatives (literal, related, and stylistic), not duplicates.

Query:

```text
{{QUERY}}
```
