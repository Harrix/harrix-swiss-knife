You are performing optical character recognition on a scan image.

Extract all visible text from the image accurately. Preserve the original paragraph structure: separate paragraphs with a blank line when the layout indicates distinct blocks.

Rules:

- Support Russian and English text.
- Do not add comments, headings, markdown formatting, or image links.
- Do not translate the text.
- Fix obvious OCR-style character confusions only when the intended word is unambiguous from context.
- If no text is found, return an empty response.

Return only the recognized plain text.
