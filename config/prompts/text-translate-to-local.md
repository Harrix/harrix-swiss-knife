Translate the following text into {{LOCAL_LANGUAGE}} (code: {{LOCAL_LANGUAGE_CODE}}).

Keep meaning, tone, markdown, links, and paragraph breaks. Return only the translation — no commentary, no quotes, no language labels.

Text:

{{TEXT}}
